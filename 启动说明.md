# dsh 本地启动说明

deepseek-harness 源码版（0.1.0-rc.8）手动启动指南。

## 环境要求

- Node.js ≥ 22.18（当前环境：Node 24.19.0 + pnpm 11.7.0）
- 已安装依赖（`node_modules` 存在）

## 启动步骤

```bash
# 1. 首次运行或 git pull 同步后，安装依赖并构建
pnpm install && pnpm run build

# 2. 启动 web UI（浏览器访问 http://127.0.0.1:3080/）
pnpm dsh web
# 不想自动打开浏览器可加 --no-open：
pnpm dsh web --no-open
```

## 验证是否启动成功

```bash
netstat -ano | grep 3080        # 看到 LISTENING 即为成功
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:3080/   # 返回 200
```

## 注意事项

- `dsh` 是根 `package.json` 的 script（`node --import tsx/esm apps/cli/src/bin.ts`），不是独立的 bin 命令
- 运行中的 dsh web 常驻约 **3.2GB 内存**，注意内存占用
- 会话数据持久化在 `~/.dsh/sessions`（JSONL + zstd）
- 局域网/远程访问默认只监听 127.0.0.1
- 停止服务：Ctrl+C 或结束对应 node 进程

## 环境信息

- 本地源码版本：0.1.0-rc.8（master 分支）
- npm 官方包 `@deepseek-ai/dsh` 已发布到 0.1.0-rc.7
- ⚠️ npm 上另有无关的 `dsh` 1.0.1 包，**不要**用 `npm i dsh` 安装
